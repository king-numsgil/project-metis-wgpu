{
  "name": "bun-webgpu-rs",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "license": "MIT",
  "files": [
    "index.d.ts",
    "index.js"
  ],
  "napi": {
    "binaryName": "bun-webgpu-rs",
    "targets": [
      "x86_64-pc-windows-msvc",
      "x86_64-apple-darwin",
      "x86_64-unknown-linux-gnu",
      "aarch64-apple-darwin"
    ]
  },
  "engines": {
    "node": ">= 12.22.0 < 13 || >= 14.17.0 < 15 || >= 15.12.0 < 16 || >= 16.0.0"
  },
  "scripts": {
    "build": "napi build --platform --release --no-const-enum",
    "postbuild": "node scripts/prepend-dts-header.mjs",
    "build:debug": "napi build --platform --no-const-enum",
    "postbuild:debug": "node scripts/prepend-dts-header.mjs",
    "version": "napi version"
  },
  "devDependencies": {
    "@emnapi/core": "^1.5.0",
    "@emnapi/runtime": "^1.5.0",
    "@napi-rs/cli": "^3.2.0",
    "@oxc-node/core": "^0.1.0",
    "@tybys/wasm-util": "^0.10.0",
    "@types/bun": "latest",
    "typescript": "^7"
  }
}
